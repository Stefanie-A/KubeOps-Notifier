services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - application-network

  backend:
    build: ./backend
    ports:
      - "3500:3500"
    environment:
      - DB_URL=${DB_URL}
    networks:
      - application-network
  
networks:
    application-network:
      driver: bridge
