apiVersion: v1
kind: Service
metadata:
  name: kubeops-notifier
  namespace: kubeops
  labels:
    app: kubeops-notifier
spec:
  selector:
    app: kubeops-notifier
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP